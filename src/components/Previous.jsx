
export const Previous = ( {handlePreviousFunc} ) => { 
  return (
    <li className="page-item">
        <button className="btn btn-primary" onClick={ handlePreviousFunc }>
            Previous
        </button>
    </li>
 )
}
